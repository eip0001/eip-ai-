{
  "name": "eip-ai",
  "version": "0.1.0",
  "description": "EIP.AI — autonomous intelligence. site + escrow marketplace on Base.",
  "scripts": {
    "compile":       "hardhat compile",
    "test":          "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.cjs --network baseSepolia",
    "deploy:base":   "hardhat run scripts/deploy.cjs --network base",
    "verify":        "hardhat verify"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "hh2",
    "dotenv": "^16.4.5"
  }
}
